{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy || echo 'No migrations' && npm run build",
  "outputDirectory": ".next",
  "devCommand": "npm run dev",
  "ignoreCommand": "echo 'Vercel: using all files'",
  "github": {
    "silent": true
  }
}